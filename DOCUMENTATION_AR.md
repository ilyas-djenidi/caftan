# توثيق مشروع قفطان (Caftan E-commerce)

هذا الملف يحتوي على شرح مفصل للمنطق البرمجي، البنية المعمارية (Architecture)، قواعد البيانات، ودليل تشغيل المشروع محلياً (للمطور أو الشخص الذي سيقوم برفع المشروع على الاستضافة).

---

## 1. نظرة عامة على المشروع (Project Overview)
المشروع عبارة عن متجر إلكتروني متكامل لبيع الأزياء (قفطان، حقائب، إكسسوارات، جلابات) ويحتوي على قسمين رئيسيين:
1. **واجهة المستخدم (Storefront):** تتيح للزبائن تصفح المنتجات، إضافة المنتجات للسلة، وإتمام عمليات الشراء (Checkout).
2. **لوحة تحكم الإدارة (Admin Dashboard):** تتيح للمشرفين إدارة الطلبات، المنتجات، الحزم (Packs)، العروض، التقييمات، ومتابعة رسائل الزوار.

---

## 2. البنية المعمارية والمنطق البرمجي (Architecture & Logic)

يعتمد المشروع على بنية **Client-Server Architecture** وفصل كامل بين الواجهة الأمامية (Frontend) والخلفية (Backend) للتواصل عبر واجهات برمجة التطبيقات (RESTful APIs).

### الواجهة الأمامية (Frontend)
- **إطار العمل:** مبنية باستخدام **React 19** و **Vite** للحصول على أداء سريع وسلس.
- **التصميم وتجربة المستخدم:** تم استخدام **Tailwind CSS** للتنسيق المتجاوب مع جميع الشاشات، و **Framer Motion** لإضافة حركات بصرية جذابة للمستخدم (Animations).
- **إدارة الحالة (State Management):** تعتمد على مكتبة **Zustand** الخفيفة والسريعة لإدارة حالة التطبيق (مثل سلة المشتريات ومعلومات المستخدم).
- **المنطق (Logic):** 
  - يتم استخدام **React Router v7** للتنقل بين الصفحات. الصفحات يتم تحميلها تدريجياً (Lazy Loading) لضمان سرعة تحميل الموقع في البداية.
  - يدعم الموقع تعدد اللغات والاتجاهات (RTL / LTR) عبر مكتبة `react-i18next`.

### الواجهة الخلفية (Backend)
- **بيئة العمل:** **Node.js** مع إطار عمل **Express.js**.
- **المنطق (Logic):**
  - **نمط MVC المبسط:** مقسم إلى (Routes) لتوجيه الطلبات، (Controllers) لمعالجة الطلب، و (Services) للتعامل مع منطق الأعمال وقاعدة البيانات.
  - **الأداء العالي (Cluster Mode):** في بيئة الإنتاج (Production)، يقوم الخادم بتشغيل عدة عمليات (Workers) على حسب عدد أنوية المعالج (CPU) لتوزيع الحمل وتحمل عدد كبير من الزوار في نفس الوقت.
  - **رفع الملفات:** يستخدم **Multer** لاستقبال الصور و **Sharp** لضغط ومعالجة الصور قبل حفظها لتوفير مساحة التخزين وزيادة سرعة التحميل.
  - **الأمان:** المصادقة تتم عبر **JWT** (JSON Web Tokens) ويتم تشفير كلمات المرور باستخدام **Bcrypt**.
  - **نظام التوصيل:** مدمج مع واجهة التوصيل الخارجية (Guepex Delivery API).

---

## 3. قواعد البيانات (Database & Caching)

النظام يعتمد على قاعدتي بيانات لضمان السرعة والموثوقية:

1. **PostgreSQL (قاعدة البيانات الأساسية):**
   - تم اختيارها كقاعدة بيانات علاقية قوية.
   - **الاتصال:** يتم الاتصال بها مباشرة باستخدام تعريفات `pg` (بدون استخدام ORM مثل Prisma أو Sequelize) لضمان كتابة استعلامات (SQL) سريعة ومحسنة جداً.
   - **أهم الجداول (Schema):**
     - `admins`: بيانات المشرفين على الموقع.
     - `products`, `product_images`, `product_attributes`: تفاصيل المنتجات وصورها وخصائصها (مثل المقاس واللون).
     - `orders`, `order_items`: تفاصيل طلبات الزبائن.
     - `packs`, `pack_items`: نظام الحزم (عروض تجميعية لعدة منتجات).
     - `promo_codes`: أكواد الخصم.
     - `reviews`: تقييمات الزوار للمنتجات.
     - `messages`: رسائل نموذج "اتصل بنا".
     - `hero_banners`, `site_settings`: إعدادات الموقع والبانرات في الصفحة الرئيسية.

2. **Redis (للتخزين المؤقت - Caching):**
   - يستخدم لتخزين البيانات التي تُطلب بكثرة (مثل الإعدادات والمنتجات الشائعة) لتسريع استجابة الخادم وتخفيف الضغط على PostgreSQL.
   - يستخدم أيضاً لنظام (Rate Limiting) لمنع هجمات حجب الخدمة (DDoS) عن طريق تحديد عدد الطلبات المسموح بها لكل مستخدم.

---

## 4. كيفية تشغيل المشروع محلياً (How to Run Locally)

لتتمكن أنت أو صديقك من تشغيل وفحص المشروع على الكمبيوتر الشخصي، يرجى اتباع الخطوات التالية:

### المتطلبات الأساسية (Prerequisites)
1. تثبيت **Node.js** (الإصدار 18 أو أحدث).
2. تثبيت **PostgreSQL** والتأكد من تشغيله في الخلفية.
3. تثبيت **Redis** والتأكد من تشغيله في الخلفية.

### الخطوة الأولى: إعداد قاعدة البيانات (PostgreSQL)
- قم بفتح PostgreSQL (عبر PGAdmin أو موجه الأوامر psql) وقم بإنشاء قاعدة بيانات جديدة فارغة، وسمّها مثلاً: `caftan_db`.

### الخطوة الثانية: تشغيل الخادم (Backend)
1. افتح موجه الأوامر (Terminal) وانتقل إلى مجلد `backend`:
   ```bash
   cd backend
   ```
2. تأكد من إعداد ملف `.env` في مجلد الـ `backend`. يمكنك نسخه من `.env.example` أو إدخال البيانات التالية كحد أدنى:
   ```env
   NODE_ENV=development
   PORT=4000
   BASE_URL=http://localhost:4000

   # Database settings (تأكد من مطابقتها لإعدادات جهازك)
   DB_HOST=127.0.0.1
   DB_PORT=5432
   DB_NAME=caftan_db
   DB_USER=postgres
   DB_PASSWORD=ضع_باسورد_قاعدة_البيانات_هنا

   # Redis settings
   REDIS_HOST=127.0.0.1
   REDIS_PORT=6379

   # JWT settings
   JWT_SECRET=super_secret_key_change_me
   ```
3. تثبيت الحزم (Dependencies):
   ```bash
   npm install
   ```
4. إنشاء الجداول في قاعدة البيانات:
   ```bash
   npm run migrate
   ```
5. إنشاء حساب المدير الافتراضي (لكي تستطيع تسجيل الدخول لاحقاً للوحة التحكم):
   ```bash
   npm run seed
   ```
6. تشغيل خادم الباك إند:
   ```bash
   npm run dev
   ```
   *سيعمل الخادم الآن على الرابط: http://localhost:4000*

### الخطوة الثالثة: تشغيل واجهة المستخدم (Frontend)
1. افتح نافذة موجه أوامر جديدة (Terminal) وانتقل إلى مجلد `frontend`:
   ```bash
   cd frontend
   ```
2. تأكد من أن ملف `.env` في هذا المجلد يحتوي على الرابط الصحيح للباك إند:
   ```env
   VITE_API_URL=http://localhost:4000
   ```
3. تثبيت الحزم:
   ```bash
   npm install
   ```
4. تشغيل الواجهة الأمامية:
   ```bash
   npm run dev
   ```
   *سيقوم Vite بإعطائك رابط (مثل http://localhost:5173) لفتح الموقع في المتصفح.*

---
**ملاحظة للمستضيف (Host):** عند رفع المشروع على خادم إنتاج (Production Server مثل VPS):
- يجب التأكد من تشغيل `npm run build` في الـ frontend ورفع مجلد `dist`.
- في الـ backend، يجب استخدام أداة مثل `PM2` لإبقاء السيرفر يعمل في الخلفية وتفعيل الـ (Cluster Mode).
- تأكد من تعديل متغيرات البيئة `.env` لتكون آمنة، وضبط `NODE_ENV=production`.
